namespace UIFramework
{
    /// <summary>
    /// UI Context 运行状态。
    /// </summary>
    public enum UIContextState
    {
        None,
        Loading,
        Shown,
        Hidden,
        Closed,
        Destroyed,
    }
}
